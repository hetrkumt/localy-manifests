apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: platform-secret-store
  namespace: cart-service
  annotations:
    localy.io/phase: "7-correction"
    localy.io/allowed-sm-path: "/localy/prod/workload/cart-*"
    localy.io/irsa-role: "prod-eks-cart-service-irsa-role"
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-northeast-2
      auth:
        jwt:
          serviceAccountRef:
            name: cart-service-sa
