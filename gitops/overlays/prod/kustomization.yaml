# Prod overlay — Gate 8A GitOps SSOT Root (dual App-of-Apps)
# Wired by l4-bootstrap helm_release.argocd_apps → path: gitops/overlays/prod
# Gate 8B (prune:true) and 8C (_archive) deferred until infra restore + soak.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
commonAnnotations:
  localy.io/environment: "prod"
  localy.io/cutover-mode: "gate-8a"
  localy.io/phase: "8"
