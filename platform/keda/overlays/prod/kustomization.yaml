# Phase 2: helmCharts RETIRED — see gitops/platform-apps/keda.yaml
# Extra RBAC (Rollout scale) is synced as a directory source on the KEDA App.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rbac-rollouts.yaml
