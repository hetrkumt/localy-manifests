# Phase 2: helmCharts RETIRED — Bitnami chart via Multi-Source Application
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # keycloak-tgb.yaml retired — Ingress (localy-external-alb-keycloak) + LBC
  # creates TargetGroups. Readiness gate is namespace-injected.

configMapGenerator:
  - name: keycloak-realm-config
    namespace: auth-namespace
    files:
      - realm-import.json
    options:
      disableNameSuffixHash: true
