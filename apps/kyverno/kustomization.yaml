apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - network-policy.yaml
  - policies/kyverno-policy.yaml
  - policies/protect-alertmanager.yaml
  - policies/prevent-cross-namespace-secrets.yaml
  - policies/restrict-image-registries.yaml
  - policies/verify-cosign-signature.yaml
  # Deferred: Rollout CRD is not installed yet (argo-rollouts App).
  # Re-add after `kubectl get crd rollouts.argoproj.io` succeeds.
  # - policies/enforce-rollout-probes.yaml
  # - policies/restrict-rollout-traffic-routing.yaml
