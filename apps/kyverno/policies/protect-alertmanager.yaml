apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: protect-alertmanager-irsa-identity
  annotations:
    policies.kyverno.io/title: Protect Alertmanager IRSA Identity
    policies.kyverno.io/category: DevSecOps
    policies.kyverno.io/severity: high
spec:
  validationFailureAction: Enforce
  failurePolicy: Fail
  webhookTimeoutSeconds: 3
  background: true
  rules:
    - name: deny-non-official-alertmanager-images
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - monitoring
              operations:
                - CREATE
                - UPDATE
      preconditions:
        all:
          - key: "{{ request.object.spec.serviceAccountName || '' }}"
            operator: Equals
            value: alarm-pipeline-sns-publisher
      validate:
        message: >-
          Pods with serviceAccountName 'alarm-pipeline-sns-publisher' must only
          use authorized Alertmanager or config-reloader images. IRSA identity
          hijacking is denied.
        anyPattern:
          - spec:
              containers:
                - image: "quay.io/prometheus/alertmanager:* | quay.io/prometheus-operator/prometheus-config-reloader:*"
              =(initContainers):
                - image: "quay.io/prometheus/alertmanager:* | quay.io/prometheus-operator/prometheus-config-reloader:*"
    - name: deny-pinpoint-container-image-mismatch
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - monitoring
              operations:
                - CREATE
                - UPDATE
      preconditions:
        all:
          - key: "{{ request.object.spec.serviceAccountName || '' }}"
            operator: Equals
            value: alarm-pipeline-sns-publisher
      validate:
          message: Container name must match its authorized image (alertmanager or config-reloader only). IRSA identity hijacking is denied.
          foreach:
          # 1. alertmanager ?대쫫?몃뜲 怨듭떇 ?대?吏媛 ?꾨땶 寃쎌슦 李⑤떒
          - list: request.object.spec.containers
            deny:
              conditions:
                all:
                - key: '{{ element.name }}'
                  operator: Equals
                  value: alertmanager
                - key: '{{ element.image }}'
                  operator: NotEquals
                  value: quay.io/prometheus/alertmanager:*

          # 2. config-reloader ?대쫫?몃뜲 怨듭떇 ?대?吏媛 ?꾨땶 寃쎌슦 李⑤떒
          - list: request.object.spec.containers
            deny:
              conditions:
                all:
                - key: '{{ element.name }}'
                  operator: Equals
                  value: config-reloader
                - key: '{{ element.image }}'
                  operator: NotEquals
                  value: quay.io/prometheus-operator/prometheus-config-reloader:*

          # 3. ?꾩삁 ?덉슜?섏? ?딆? ?됰슧??而⑦뀒?대꼫 ?대쫫??寃쎌슦 李⑤떒
          - list: request.object.spec.containers
            deny:
              conditions:
                all:
                - key: '{{ element.name }}'
                  operator: NotIn
                  value:
                  - alertmanager
                  - config-reloader

          # 4. initContainers: alertmanager ?대쫫?몃뜲 怨듭떇 ?대?吏媛 ?꾨땶 寃쎌슦 李⑤떒
          - list: request.object.spec.initContainers
            deny:
              conditions:
                all:
                - key: '{{ element.name }}'
                  operator: Equals
                  value: alertmanager
                - key: '{{ element.image }}'
                  operator: NotEquals
                  value: quay.io/prometheus/alertmanager:*

          # 5. initContainers: config-reloader ?대쫫?몃뜲 怨듭떇 ?대?吏媛 ?꾨땶 寃쎌슦 李⑤떒
          - list: request.object.spec.initContainers
            deny:
              conditions:
                all:
                - key: '{{ element.name }}'
                  operator: Equals
                  value: config-reloader
                - key: '{{ element.image }}'
                  operator: NotEquals
                  value: quay.io/prometheus-operator/prometheus-config-reloader:*

          # 6. initContainers: ?꾩삁 ?덉슜?섏? ?딆? ?됰슧??而⑦뀒?대꼫 ?대쫫??寃쎌슦 李⑤떒
          - list: request.object.spec.initContainers
            deny:
              conditions:
                all:
                - key: '{{ element.name }}'
                  operator: NotIn
                  value:
                  - alertmanager
                  - config-reloader
