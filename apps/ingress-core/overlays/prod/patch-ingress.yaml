apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-platform-ingress
  namespace: default
  annotations:
    alb.ingress.kubernetes.io/wafv2-acl-arn: "<PLACEHOLDER_WAF_ACL_ARN>"
    alb.ingress.kubernetes.io/certificate-arn: "<PLACEHOLDER_ACM_CERT_ARN>"
