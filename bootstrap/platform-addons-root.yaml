# DEPRECATED (Phase 8 Gate 8A) — not wired by l4-bootstrap.
# Physical move to _archive/bootstrap/ deferred to Go-Live (8C).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-addons-root
  namespace: argocd
  annotations:
    localy.io/prune-freeze: "phase-0"
    localy.io/cutover-mode: "legacy-frozen"
    argocd.argoproj.io/sync-options: ServerSideApply=true,Delete=false,Prune=false
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "https://github.com/hetrkumt/localy-manifests.git"
    targetRevision: HEAD
    path: bootstrap/platform-apps
  destination:
    server: "https://kubernetes.default.svc"
    namespace: argocd
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - Delete=false
      - Prune=false
