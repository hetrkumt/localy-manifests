apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-addons-root
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "https://github.com/my-org/localy-manifests.git"
    targetRevision: HEAD
    path: bootstrap/platform-apps
  destination:
    server: "https://kubernetes.default.svc"
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
