apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: user-service-pdb
  namespace: user-service
  annotations:
    localy.io/phase: "5"
spec:
  maxUnavailable: 50%
  selector:
    matchLabels:
      app: user-service
