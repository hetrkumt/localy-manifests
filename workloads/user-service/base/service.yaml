apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: user-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 9001
    - name: management
      port: 9090
      targetPort: 9090
  selector:
    app: user-service
  type: ClusterIP
