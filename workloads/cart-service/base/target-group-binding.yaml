# DEPRECATED — not wired in kustomization. Direct TG binding retired (edge Ingress SSOT).
# Legacy ARN (rollback only): SSM /localy/prod/network/target_groups/cart
apiVersion: elbv2.k8s.aws/v1beta1
kind: TargetGroupBinding
metadata:
  name: cart-service-tgb
  namespace: cart-service
  annotations:
    localy.io/status: "deprecated"
spec:
  serviceRef:
    name: cart-service
    port: 8080
  targetType: ip
  targetGroupARN: "arn:aws:elasticloadbalancing:ap-northeast-2:533003975005:targetgroup/DEPRECATED/0000000000000000"
