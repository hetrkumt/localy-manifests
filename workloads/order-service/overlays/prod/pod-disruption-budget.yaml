# Phase 5 — PDB 50% survival during drain/rolling
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: order-service-pdb
  namespace: order-service
  annotations:
    localy.io/phase: "5"
spec:
  maxUnavailable: 50%
  selector:
    matchLabels:
      app: order-service
